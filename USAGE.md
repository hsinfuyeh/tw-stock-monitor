# 使用手冊

## 最常用的四件事

### 1. 開網站（平常就用這個）

```bash
cd C:\Users\USER\Claude\twse-quant
python server.py
```

然後開瀏覽器到 **http://127.0.0.1:5000**

- 頂部搜尋框可以打**代號或中文名稱**（2330 或 台積電），隨時切換股票
- 按 `/` 鍵直接跳到搜尋框，方向鍵選、Enter 進入
- 第一次啟動要等約 40 秒載入面板，之後切換股票是瞬間的
- **看不懂的名詞，滑鼠移上去就有解釋**（手機點一下）。
  帶有藍色虛線底線的字都可以查，全站共 32 條白話說明
- 右上角的 ☾ / ☀ 可切換**淺色 / 深夜模式**，選擇會記住
- **終端機視窗不能關**，關掉伺服器就停了

### 讓伺服器背景常駐（建議）

直接在終端機跑的話，視窗一關就停。要讓它一直活著：

```powershell
Start-Process python -ArgumentList "server.py" -WorkingDirectory "C:\Users\USER\Claude\twse-quant" -RedirectStandardOutput "server.out" -RedirectStandardError "server.err" -WindowStyle Hidden
```

要停掉：`netstat -ano | findstr :5000` 找出 PID，再 `taskkill /PID <PID> /F`。

**開機自動啟動**（設一次就好）：

```powershell
$a = New-ScheduledTaskAction -Execute "python" -Argument "server.py" -WorkingDirectory "C:\Users\USER\Claude\twse-quant"
Register-ScheduledTask -TaskName "twse-web" -Action $a -Trigger (New-ScheduledTaskTrigger -AtLogOn)
```

改了資料或程式之後要重啟伺服器才會生效（面板快取在記憶體裡）。

網站有兩大區：

**排行榜**（頂部「排行榜」連結）—— 用來**瀏覽發現**，不用一檔一檔搜：

| 分頁 | 內容 |
|---|---|
| 成交金額 | 今天資金集中在哪 |
| 今日漲幅 / 跌幅 | 今天漲跌最多的 |
| 爆量 | 量能相對 20 日均量放大最多 |
| 創20日新高 / 新低 | 突破或跌破近期區間 |
| 低波動 | 波動最小的高流動性股票 |
| 即將除權息 | 未來 60 天的除權息行事曆 |
| 法人買超 / 賣超 | 三大法人今日買賣超（已用成交量正規化）|
| **體質篩選** | **每天從這裡開始**。套用兩條驗證過的排除規則後，依歷史勝率等級排序 |
| 綜合評分 | 因子合成排序（**已驗證為反指標，頁面上有紅色警告**） |

所有榜單都排除 20 日均額低於 2,000 萬的標的。點代號直接跳個股頁。

**個股頁**會顯示四塊：

0. **歷史勝率等級（1–5）** —— 「歷史上條件相似的案例，有多少比例在後續 10 日
   跑贏市場中位數」。等級 1→5 對應 46.2% → 53.6%。
   **這是查表不是預測**，用途是排優先順序。

1. **進場前先確認這幾件事** —— 流動性夠不夠你的部位、波動你能不能承受、
   近期有沒有漲跌停、有沒有除權息、價格在什麼位置
2. **最近 30 個交易日 K 線**
3. **那些技術型態能告訴你什麼** —— 六種常見型態的真實資訊量，
   與交易成本並列比較（圖上的黃線就是成本門檻）

**這個頁面刻意不給「可以買 / 不要買」。** 這套系統已經用真實資料驗證過無法預測，
給結論等於把一個無效的東西包裝成建議。它負責讓你不漏看該看的，判斷還是你做。

### 2. 每天更新（一行搞定）

```bash
cd C:\Users\USER\Claude\twse-quant
python run.py daily
```

它會做完：抓最近 15 個交易日 → 重建倉儲 → 產生全部報告 → 更新總覽頁。
約 3–5 分鐘。跑完打開 `reports/index.html` 就是入口。

台股 14:30 後才有當日盤後資料，所以建議晚上跑。

### 3. 看某一檔個股（產生靜態檔，可離線保存）

```bash
python run.py stock 2330 2317 2454
```

產出 `reports/stock_2330.html` 等，內容是 30 日 K 線、成交量、六種規則、
黃色圓點、命中後 1/3/5/10 日報酬、歷史案例統計。

ETF 也可以（代號以 00 開頭會自動走 ETF 宇宙）：

```bash
python run.py stock 0050 0056
```

### 4. 自己問問題

倉儲是 DuckDB，任何橫斷面問題都能用一行 SQL 問：

```bash
python -c "import store; print(store.q('''
  SELECT code, name, close, amount/1e8 AS 成交額億
  FROM quotes WHERE date='2026-09-11' AND cat='common'
  ORDER BY amount DESC LIMIT 15'''))"
```

可用的表：`quotes`（行情）、`exrights`（除權息）、
`inst`（三大法人）、`valuation`（估值）、`margin`（融資券）。
後三個要等回補完成。

---

## 全部指令

| 指令 | 用途 | 耗時 |
|---|---|---|
| `python server.py` | **開網站**，可搜尋切換股票 | 啟動 40 秒 |
| `python run.py daily` | 每天更新資料：更新＋重建＋出全部報告 | 3–5 分 |
| `python run.py stock 2330` | 個股 K 線報告（可帶多個代號） | 40 秒 |
| `python run.py rank` | 只重產普通股排序 | 1 分 |
| `python run.py rank-etf` | 只重產 ETF 排序 | 40 秒 |
| `python run.py index` | 只重產總覽頁 | 3 秒 |
| `python run.py validate` | 框架自檢＋因子驗證表 | 3–5 分 |
| `python run.py ingest` | 續跑回補（冪等，可隨時中斷） | 視進度 |
| `python run.py build` | 只從 raw/ 重建倉儲 | 1–2 分 |
| `python tests.py` | 36 項資料層正確性測試 | 2 分 |
| `python verify.py` | 29 項統計計算驗證 | 1 分 |

---

## 報告怎麼看

### 總覽頁 `reports/index.html`
入口。資料涵蓋範圍、三個報告入口、已產生的個股清單、已驗證的結論、回補進度。

### 排序頁 `ranking.html` / `ranking_etf.html`
**先看最上面的成績單橫幅，再看名單。** 橫幅有三種：

- 灰色「樣本不足」→ 獨立期數 < 100，名單沒有統計依據
- 紅色「未通過驗證」→ 扣成本後未達顯著
- 紅色「方向相反」→ 有預測力但方向是反的（目前就是這個）
- 綠色「已通過驗證」→ 才可以參考

接著看**十分位剖面**。只看 D9−D0 一個數字會誤導，要看十個分位的形狀：

- 單調遞增 → 訊號健康
- 單調遞減 → 方向相反
- 沒有形狀 → 兩端價差那個數字不可靠

名單裡點代號可以跳進該檔的 K 線報告。

### 個股報告（網站的個股頁 / `stock_XXXX.html`）

「歷史案例統計」表格的大字是**規則的真實資訊量**，已經扣掉兩層基準：

1. **扣大盤** —— 多頭期間任何規則的絕對報酬都是正的，那是大盤漲跌
2. **扣這檔股票自身的漂移** —— 這層最容易漏。強勢股沒命中規則的日子表現也很好，
   不扣掉的話六條規則會全都看起來有效

小字保留「命中 / 未命中」的原始數字，可以自己看出差距從哪來。

**判斷方式：** 把資訊量跟 0.6% 的來回交易成本比。小於成本就等於沒有可操作性——
就算方向猜對也賺不回手續費和證交稅。網站的訊號條圖上那兩條黃線就是成本門檻。

案例數 < 30 的統計會直接標示「不具統計意義」。

---

## 設定每天自動跑（Windows 工作排程器）

```powershell
$action  = New-ScheduledTaskAction -Execute "python" -Argument "run.py daily" -WorkingDirectory "C:\Users\USER\Claude\twse-quant"
$trigger = New-ScheduledTaskTrigger -Daily -At 20:00
Register-ScheduledTask -TaskName "twse-quant-daily" -Action $action -Trigger $trigger -Description "台股量化倉儲每日更新"
```

移除：

```powershell
Unregister-ScheduledTask -TaskName "twse-quant-daily" -Confirm:$false
```

假日不開盤時 `daily` 會抓到空資料並自動略過，不會出錯，所以每天跑沒問題。

---

## 遇到問題

**回補中斷了** — 直接再跑 `python run.py ingest`，已抓過的會跳過。

**想改因子** — 改 `run.py` 的 `CORE`，然後 `python run.py validate` 看結果。
改完務必重跑 `python verify.py` 確認計算層沒被改壞。

**改了解析邏輯** — 不用重抓，原始檔都在 `raw/`，跑 `python run.py build` 重建即可。

**報告數字看起來怪** — 先跑 `python tests.py` 和 `python verify.py`。
60 項測試涵蓋正負號、除權息、look-ahead、IC、十分位、Newey-West 等。

---

## 目前的狀態（重要）

**通過驗證、可以用的（2026-09 更新）：**

兩條排除規則（都通過 Bonferroni 多重檢定校正）：

```
排除殖利率最低 40%   -> 剩餘池「中位數」+5.49%/年   t=4.69  p<0.0001
排除價格位置最低 30% -> 剩餘池「平均數」+2.93%/年   t=2.96  p=0.0018
```

**排除的成本門檻是 0**（不買某檔不用付錢），選擇要先跨過 0.6% 手續費——
這是排除法比選股法容易的根本原因。

歷史勝率分級（兩條規則的成分等權合成）：

```
等級 1 → 5 的歷史勝率  46.2% → 48.3% → 50.4% → 51.3% → 53.6%
梯度 +6.84pp   t=4.40   五級全單調   前後半樣本各自成立
```

**還不能用的部分：**
- 看漲／看跌名單（綜合評分）。在 2019–2026 樣本上是**穩定的反指標**
  （十分位單調性 −0.61、t ≈ −4.4），不要照著下單。
- 任何「一到兩週會漲」的預測。測過 20+ 個訊號，沒有一個扣掉成本後還是正的。

**關於法人買超（唯一的真訊號）：**

三大法人買超是全系統**唯一通過「十分位單調 + 樣本外仍為正」的訊號**：

```
十分位剖面   D0 -0.10 → D9 +0.20   單調性 +0.88
樣本外驗證   毛價差 +0.228%（5 日）  ← 出了樣本還是正的
多重檢定     p=0.0116 vs 門檻 0.0083  → 擦邊未通過
扣交易成本   淨 -0.643%，年化 -32%
```

**它是真的有資訊，但效果（+0.23%）小於 5 日換倉的成本（0.97%）。**
所以網站把它當「參考資訊」呈現——「今天法人在買這檔」是有意義的事實，
但不能拿來當進出訊號。

**已經用真實資料驗證的結論：**
1. 六條 K 線規則的超額報酬全部趨近於零，多重檢定後無一通過
2. 10 日換倉光成本就吃掉年化約 25%，實測最佳毛價差年化僅約 22%
3. 因子的滾動 IC 自相關接近 0、符號持續率 44–56%（等同丟銅板），
   所以「用最近表現好的因子」這種動態加權是在對雜訊擬合
4. IC 與十分位價差會給出相反結論：低波動股「贏的次數多」但「輸在平均」，
   因為高波動股有樂透式肥尾。只看其中一個必然誤判
