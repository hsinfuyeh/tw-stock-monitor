# 資料倉儲與原始檔封存 —— 刻意不進版控
#
# data/  DuckDB 873 MB，單檔就超過 GitHub 100 MB 的硬限制，push 會被直接擋。
# raw/   8,071 個原始檔共 626 MB。
#
# 這兩個都是「可重現的衍生物」：raw/ 隨時能從 TWSE 重抓（ingest 是冪等的），
# data/ 完全由 raw/ 重建（store.build 從原始檔全量重建）。
# 版控該保存的是「怎麼產生」的程式碼，不是產生出來的東西。
data/
raw/

# 產生的報告（由 run.py 產出，可重現）
reports/

# 執行期產物
__pycache__/
*.pyc
*.log
*.err
*.out

# 改版前的手動備份。沒有版控時這是唯一的退路，有了 git 之後就該退場，
# 留著只會讓人不確定哪一份才是真的。
*.bak

# 產生的網站資料。不進版控的理由跟 data/ 不同 ——
# 這個是「每天都會變」：18 MB x 250 個交易日 ≈ 一年 4.5 GB，而且 git 歷史刪不掉。
# 部署走 actions/upload-pages-artifact，直接把產物丟給 Pages，不經過 repo。
# site/ 底下的 HTML/CSS/JS 是要進版控的，只排除 data/。
site/data/
